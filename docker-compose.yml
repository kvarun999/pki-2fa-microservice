version: "3.8"
services:
  app:
    build: .
    ports: ["8080:8080"]
    volumes:
      - ./data:/data
      - ./cron:/cron
      # Mount keys so container can read them
      - ./student_private.pem:/app/student_private.pem:ro
    environment:
      - TZ=UTC
